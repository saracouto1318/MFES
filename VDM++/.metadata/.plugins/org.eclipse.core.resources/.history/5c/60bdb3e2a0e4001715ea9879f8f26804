class Patient is subclass of Person

types
	public String = seq of (char);
instance variables
  healthNumber: String;
  
operations
	public Patient: String ==> Patient
		Patient(n) == ( name := n; return self)
	post name = n;
	
	pure public getName : () ==> String
		getName() == (return name);
		

end Patient
