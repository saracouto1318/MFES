class Medicament

instance variables
  private name:Utils`String;
operations
	public Medicament: Utils`String ==> Medicament
		Medicament(n) == (name := n; return self)
	post name = n;
	
	pure public getName: () ==> Utils`String
		getName() == (return name);

end Medicament
