class Specialty

instance variables
  private name: Utils`String;
operations
	public Specialty : Utils`String ==> Specialty
		Specialty(n) == (name := n; return self)
	post name = n;
		
	pure public getName : () ==> Utils`String
		getName() == (return name);

end Specialty
