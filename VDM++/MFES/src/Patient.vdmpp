class Patient is subclass of Person
instance variables
  private healthNumber: Utils`String;
operations
	public Patient: Utils`String * Utils`String * Utils`String * Utils`String * Utils`String * Utils`String ==> Patient
		Patient(a, fn, ln, c, pn, n) == ( healthNumber := n; Person(a, fn, ln, c, pn))
	post healthNumber = n;
	
	pure public getHealthNumber : () ==> Utils`String
		getHealthNumber() == (return healthNumber);

end Patient
